# PostgreSQL connection used by Prisma.
DATABASE_URL="postgresql://invoice:invoice@localhost:5432/invoice?schema=public"

# Optional shared secret for internal access.
# When empty, API and UI are open (local development only).
# When set, send header: Authorization: Bearer <token>
APP_ACCESS_TOKEN=""
