# PostgreSQL (see docker-compose.yml)
DATABASE_URL="postgresql://invoice:invoice@localhost:5432/invoice_creation?schema=public"

# Optional shared-secret gate for internal access.
# Leave empty in local development to disable.
INTERNAL_APP_SECRET=""
